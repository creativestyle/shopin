/** Default revalidate time in seconds for content (layout, pages) when fetching from backend. */
export const DEFAULT_CONTENT_REVALIDATE_SECONDS = 60

/** Revalidate time for main navigation. */
export const NAVIGATION_REVALIDATE_SECONDS = 60

/** Revalidate time for product page data. */
export const PRODUCT_PAGE_REVALIDATE_SECONDS = 60

/** Revalidate time for product collection page data. */
export const PRODUCT_COLLECTION_PAGE_REVALIDATE_SECONDS = 60

/** Revalidate time for store config. */
export const STORE_CONFIG_REVALIDATE_SECONDS = 3600

/** Revalidate time for i18n translations. */
export const I18N_REVALIDATE_SECONDS = 3600
