/**
 * Default cookie configuration constants
 * These are general cookie settings used across the application
 */
export const DEFAULT_COOKIE_PATH = '/'
export const DEFAULT_COOKIE_SAME_SITE = 'strict' as const
export const COOKIE_CLEAR_MAX_AGE = 0
