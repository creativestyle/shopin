# @core/prettier-config

Shared Prettier configuration (base and CJS export) for the monorepo.

## Usage

```js
// ESM
import config from "@core/prettier-config/base";

// CJS
const config = require("@core/prettier-config/base.cjs");
```

Other packages extend this for consistent formatting.
