# @core/typescript-config

Shared TypeScript base config (Node 22) for apps and packages.

## Usage

Extend in `tsconfig.json` via `"extends": "@core/typescript-config/tsconfig.json"` (or the path to the base config you use). Provides a common base for the monorepo.
