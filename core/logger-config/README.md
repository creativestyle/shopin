# @core/logger-config

Shared Pino logger configuration (log level, pretty print) for BFF and presentation.

## Usage

Import helpers (e.g. `getLogLevel`, `isPrettyPrintEnabled`, formatters) from `@core/logger-config`. Env: `LOG_LEVEL`, `LOG_PRETTY_PRINT` (see root `.env.example`).
