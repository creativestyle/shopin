# @core/i18n

Internationalization module with translation files and configuration.

## Usage

Import from `@core/i18n` in apps (e.g. presentation) for translations and language utilities. Run `npm run setup` or `npm run build` from root to compile.
