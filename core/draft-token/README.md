# @core/draft-token

Utilities to sign and verify draft/preview tokens (e.g. for Contentful draft mode).

## Usage

Import from `@core/draft-token`. Used by the presentation app and BFF for draft/preview URLs. Build with `npm run build` (or `npm run setup` from root).
