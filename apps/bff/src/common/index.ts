/**
 * Common BFF helpers and utilities not tied to any feature.
 * Use this module for shared helpers (e.g. formatting, parsing, guards)
 * that can be used across multiple features or infrastructure.
 */

export type { CookieOptions } from './cookie-options.interface'
export { createLoggerConfig } from './logger/logger.config'
